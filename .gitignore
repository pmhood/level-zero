# dependencies
node_modules/
.pnpm-store/

# build output (next-env.d.ts is regenerated by `next build`)
next-env.d.ts
dist/
build/
.next/
out/
*.tsbuildinfo

# caches
.turbo/
.eslintcache
coverage/

# local development asset storage (see @level-zero/storage)
.data/

# environment
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# claude code local state (agent worktrees, session scratch)
.claude/*
# ...but keep the repo-level overlay that /do and /do-next read
!.claude/do-overlay.md

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
